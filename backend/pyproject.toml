[project]
name = "afiip-backend"
version = "0.1.0"
description = "AI Financial Intelligence Infrastructure Platform — Backend API"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
